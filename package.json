{
  "name": "accelerated-execution",
  "version": "0.1.0",
  "private": true,
  "description": "AI-assisted workflow acceleration for Adobe After Effects.",
  "scripts": {
    "analyze": "node sidecar/cli.mjs analyze",
    "check": "node scripts/check.mjs && node --test",
    "doctor": "node sidecar/cli.mjs doctor",
    "proxy": "node sidecar/cli.mjs proxy",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
